package com.example.cognitiveexercisesapp.ui.components

import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier


/*// Shows initial page. This is just a PH.
@Composable
fun AppEntry(modifier: Modifier = Modifier) {
    var started by remember { mutableStateOf(false) }
    if (!started) {
        mainScreenObj.StartScreen(
            onStartClicked = { started = true },
            modifier = modifier
        )
    } else {
        mainScreenObj.MainScreen()
    }
}*/
